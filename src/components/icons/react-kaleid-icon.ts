import * as React from 'react';
import { KaleidIcon } from './kaleid-icon';
import { createComponent } from '@lit-labs/react';

export const ReactKaleidIcon = createComponent(
  React,
  'kaleid-icon',
  KaleidIcon,
  {}
);
